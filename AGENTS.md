# AGENTS.md — AI 写文章前必读

任何 AI 代理在本仓库新增或修改文章之前，必须先读完本文，再读 [CONTRIBUTING.md](CONTRIBUTING.md)（对外贡献规范，与本文规则一致）。

## 仓库定位

`awesome-chatgpt-plus-guide`：面向国内用户的 ChatGPT Plus / Pro / Codex 全中文教程仓库，持续更新。维护方 PayForChat（README 已披露）。仓库的价值在于**诚实和实用**——每篇文章都要有经核实的事实增量。

## 写作素材

- 本仓库上级目录的 `site-articles/articles/` 存有官网 146 篇文章存档（2026-08-20 快照），可作素材参考结构与事实线索。
- **个人知识库**：作者在本地维护有一份知识库，写文章前可查阅其中的**使用经验类**内容——把「AI 用法上踩过的坑、跑偏了怎么拉回来」这类有明确使用方向的教训写进文章，增加实感。**只引用使用方法，不引用信息资产**：接口实测数据、端点清单、账号信息、内部数据一律不进文章（这类内容属于敏感信息）。引用时必须先**脱敏**：抹去一切可识别信息——用户名/账号 ID、邮箱、订单号、内部网址、项目与客户名称、内部数据，改写为泛化表述（「某个项目」「一位用户」）；只带走教训和方法，不带走上任何身份线索。脱敏拿不准的内容，宁可不用。**只读取，不写入**知识库。
- **禁止照搬**：快照里的价格、政策、模型名、额度口径可能过时，写入前必须按最新官方信息核实，并在文中标注口径日期。
- 不同来源文章口径冲突时，以日期最新的来源为准（例：「免费账号不能用 Codex」是旧口径，2026-08 起免费账号可用轻量档）。

## 文章骨架（按此顺序写）

1. **frontmatter**：`title`（含目标搜索词）/ `description`（150 字内含关键词）/ `keywords`（6 个左右）/ `updated`。
2. **H1 标题**：用用户真实搜索的写法（「付款被拒」而不是「支付异常处理」）。
3. **引言 blockquote**：一句话时效口径（数据以什么为准 + 本文更新年月）。
4. **最短答案**：口语化引导 + 3 条左右编号结论，按人群分流。**引导语从下面的池子里随机轮换，禁止每篇固定同一句**：懒得看全文？重点就这几条：／如果你只想看结论：／简单来说：／总而言之，就下面几条：／先说结论：／赶时间的话，记住这几条就行：／挑重点说：／太长不看版，直接抄结论：／废话不多说，结论先放这儿：／捡要紧的说：。同卷相邻两篇不要用同一句。
5. **机制解释**：给步骤之前先用 3-5 句讲清底层机制（为什么会被拒 / 为什么会报错 / 它在背后做了什么）。只给步骤不讲为什么 = 官方文档目录式结构，禁止。
6. **主体**：对比用 Markdown 表格；步骤每步一个动作；建议带具体数字阈值（「到期前 3-5 天」「1 小时未到账走工单」），不写「尽快」「及时」。教程类必备一个可复制的「抄走即用」模块（命令块 / 配置骨架）。
7. **短板边界段**：「继续用 X 也合理的 N 种情况」——不能省，信任靠它建立。段落叫法**随机换**（丑话说在前面／泼盆冷水／XX 也有短板／什么情况其实不用／别一头热等），禁止用「公允边界」这种书面腔。
8. **FAQ**：`Q:` / `A:` 格式，5-6 条，覆盖真实性、适用边界、常见误解。
9. **参考来源**：官方文档 / 公告，真实可点的链接。
10. **相关阅读**：互链至少 2 篇已收录文章（相对路径）。

## 文件与目录

- 教程整体是一部「成长小说」：**主线是读者的五幕成长**（懂世界 00 → 认玩家 01 → 入江湖 02 → 练神功 03 → 开天眼 04），**支线三卷**（05 工具百科、06 API 与中转）从不同方向汇入主线。「充值教程」「常见报错」两卷预留，按阶段策略后续开放。
- 目录：`docs/00-ai-fundamentals/`、`01-models-and-tools/`、`02-chatgpt-getting-started/`、`03-codex-tutorials/`、`04-multimodal-creation/`、`05-ai-toolbox/`、`06-api-and-relays/`、`07-industry-watch/`、`08-chatgpt-deep-dive/`、`09-codex-advanced/`、`10-ai-for-everyone/`、`11-ai-curiosities/`。
- 文件名 = 目标关键词英文 slug + 年份（全小写连字符），如 `codex-cli-tutorial-2026.md`。**目录与文件名用规范英文（利于 SEO 与聚合站抓取），正文全中文。**
- **发布三登记**：新文章必须同时登记进 `docs/README.md`、主 `README.md` 目录和主 README 更新日志。只放文件不登记 = 用户找不到，视为未完成。
- 新文章的「相关阅读」优先互链同卷文章并回链主线（支线文章至少一条链接指回主线卷）；跨卷链接用相对路径（如 `../00-ai-fundamentals/xxx.md`）。
- **时间线纪律**：全文叙事按 2026 年当下时态写；历史事件（如 WildCard 停运、Sora 起落）明确标注年份，不与现状混写。

## 风格规则（去 AI 味）

- 短陈述句为主，一句一个信息点。
- 数字具体到个位，不写「大幅」「显著」。
- 禁用词：在……的时代、赋能、值得注意的是、综上所述、首先/其次/再次/最后。
- 第二人称克制，多用「这类场景」「重度用户」做主语。
- 观点句后必须跟证据（数字、机制、来源）。
- **叙事优先于说教**：能用场景和故事讲的不用训导句式（「你应该」「务必记住」）；翻车案例写成有情节的实录，教训从故事里自然长出来；可以有幽默和自嘲，不许有爹味。
- 标题禁用「全网最详细」「从入门到精通」「终端革命」；小标题不堆 emoji；不搞徽章墙。
- 第一人称分两种：**口语吐槽可以偶尔用**（「我第一次也懵」「说白了就是换皮罢了」「看着眼晕吧」这类自嘲和揭底，每篇至多一两处，放在段落收尾或过渡处，紧贴上下文写、不通用）；**无细节的第一人称主张禁用**（「我亲测成功」必须有时间线 / 数字支撑，否则删掉「我」）——吐槽是调味，主张要证据。

## 链接规则

- **推广外链仅限充值主题**：只有充值/订阅/额度/支付相关的文章可以自然提及代充并放 https://payforchat.com/plans?ref=github（每篇至多 1 处，紧跟「国内支付受阻」这类真实场景写，不硬塞）。教程、概念、工具、行业解读类文章**不放任何推广外链**。外部链接仅限官方文档和权威来源。
- 内链只用仓库内相对路径，只链**已收录**的文章。
- 写完必须校验：全部相对链接目标文件存在（可用脚本遍历 `](...) ` 检查）。

## 事实红线（违反整篇打回）

- 自家（PayForChat）价格**永不硬编码**进文章；OpenAI / Anthropic 官方价格可写具体数字并注更新日期。
- Pro 升级口径：只写「Plus 未到期不能直接升 Pro，等到期或用新账号」，禁止「补差价」「折算」表述。
- 模型名、额度、政策类时效信息，写入前核实，标注「2026 年 X 月口径」。
- 估算必须写明假设；所有数字必须有可点来源。
- 对比要客观：自家方案的真实短板照写，竞品优点照写。

## 写完自检清单

- [ ] frontmatter 四字段齐全
- [ ] 骨架段落齐全（尤其短板边界段、FAQ、相关阅读）
- [ ] 相关阅读 ≥2 篇且相对路径可达
- [ ] docs/README.md、主 README 目录、更新日志三处已登记
- [ ] 无推广外链，无死链
- [ ] 无禁用词、无空泛表述、无未核实数字
- [ ] 引用自个人知识库的案例已完成脱敏（无用户/网址/项目/订单等可识别信息）
- [ ] 时效信息已标口径日期

## 当前选题方向

优先使用教程类：Codex 使用方法、Codex 与 GPT/ChatGPT 概念解惑、额度机制说明、IDE 集成。充值类话题与常见报错合集后续安排。已收录文章互链见 `docs/README.md`。
